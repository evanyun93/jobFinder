# 기능설명서

이 봇이 **무엇을 하는지**. 왜 그렇게 만들었는지는 [DESIGN.md](DESIGN.md).

봇 주소와 배포 주소는 `.env` 의 `TELEGRAM_BOT_TOKEN` / `PUBLIC_BASE_URL` 에 있습니다.
현재 값은 `python -m app.main webhook` 으로 확인하세요.

---

## 1. 한 줄 요약

서울·판교 생활권의 신규 채용공고 중 **내 키워드에 맞는 것만** 골라 매일 아침
텔레그램으로 보낸다. 같은 공고는 두 번 보내지 않는다.

---

## 2. 사용자 명령

| 명령 | 하는 일 |
|---|---|
| `/start`, `/help` | 등록 + 사용법 |
| `/keywords 백엔드, GIS, PostgreSQL` | 관심 키워드 설정 (**필수**) |
| `/regions 판교, 강남` | 수집 범위 안에서 더 좁히기 |
| `/exclude 인턴, 파견, intern` | 이 단어가 있으면 제외 |
| `/my` | 현재 설정 확인 |
| `/preview` | 지금 바로 결과 보기 |
| `/more` | 오늘 잘린 나머지 공고 |
| `/stop` | 알림 중지 |

### 세부 동작

- **인자 없이 치면** 현재 값과 예시를 보여준다. `/keywords` 만 치면 지금 뭐가
  설정돼 있는지 나온다.
- **비우려면** `-` 를 준다. `/regions -` 는 지역 조건을 없앤다.
- **최대 20개**까지 받는다. 쉼표 또는 줄바꿈으로 구분한다.
- **키워드가 비어 있으면 아무것도 보내지 않는다.** 전체 발송이 아니다.
  설정 안 한 사람에게 전국 공고를 쏘면 바로 차단당하기 때문이다.
- `/preview` 는 **발송 원장에 남기지 않는다.** 미리 봐도 아침에 다시 온다.
  `/more` 는 남긴다.

---

## 3. 매일 아침 일어나는 일

```
08:00 KST (Hobby 플랜은 08:00~08:59 사이)
  │
  ├─ 1. 활성 소스에서 최근 공고 수집
  ├─ 2. 서울·판교권 밖 공고는 버림          ← DB에 들어가지도 않음
  ├─ 3. 남은 공고를 jobs 에 저장 (중복은 무시)
  ├─ 4. 사용자별로 아직 안 보낸 공고 추림
  ├─ 5. 키워드/제외/지역 필터 적용 + 점수순 정렬
  ├─ 6. 상위 12건을 텔레그램으로 발송
  └─ 7. 보낸 것만 deliveries 원장에 기록    ← 잘린 나머지는 내일 다시 후보
```

---

## 4. 매칭 규칙

정확한 semantics다. 헷갈리기 쉬운 부분이라 적어둔다.

```
1) 제외어  하나라도 걸리면 → 탈락        (제외가 항상 이긴다)
2) 지역    하나라도 걸리면 → 통과 (OR)   (비어 있으면 조건 없음)
3) 키워드  하나라도 걸리면 → 통과 (OR)   (비어 있으면 아무것도 안 보냄)
```

**대소문자 무시**, **부분문자열** 매칭이다. `python` 은 `Python`, `python3`,
`CPython` 에 다 걸린다.

### 어디를 검사하나

제목, 회사명, 근무지, 경력, 학력, 업종, **그리고 본문**을 전부 이어붙인
텍스트에서 찾는다. 본문을 넣은 이유는 제목만 보면 "백엔드 개발자" 한 줄이라
기술 키워드가 하나도 안 걸리기 때문이다.

### 정렬 순서

키워드가 **제목**에 걸리면 3점, 본문 등 다른 곳에 걸리면 1점. 합산 점수가
높은 순으로 위에 온다. 여러 키워드가 동시에 걸리는 공고가 가장 위다.

### 지역 필터의 두 층

`/regions` 는 **수집 범위 안에서 더 좁히는** 필터다. 범위 밖(예: 부산)을 적어도
그런 공고는 애초에 DB에 없다.

그룹 이름과 개별 지명 둘 다 걸린다. `/regions 판교` 도 `/regions 판교·분당` 도
동작한다.

---

## 5. 대상 지역

```
서울  /  판교·분당  /  수지·기흥  /  과천·평촌  /  서울 인접
```

| 그룹 | 포함 |
|---|---|
| 서울 | 서울 |
| 판교·분당 | 판교, 분당, 성남 |
| 수지·기흥 | 수지, 기흥 (신분당선으로 판교 연결) |
| 과천·평촌 | 과천, 평촌, 안양 |
| 서울 인접 | 하남, 광명, 구리, 남양주 |

**제외:** 처인 (용인시 처인구는 판교 생활권이 아님)

범위를 바꾸려면 [app/regions.py](app/regions.py) 의 `AREAS` 만 고치면 된다.

---

## 6. 수집 소스 현황

| 소스 | 상태 | 키 | 비고 |
|---|---|---|---|
| **Greenhouse** | ✅ 동작 중 | 불필요 | 현재 유일하게 도는 소스 |
| 워크넷 | ❌ 키 거부됨 | UUID authKey | `messageCd 002`. 활용신청 승인 확인 필요 |
| 사람인 | ⬜ 미신청 | access-key | IT 커버리지가 가장 두껍다. 우선순위 1 |
| 잡코리아 | ⬜ 미신청 | 계정별 고유 URL | 공공기관·학교 대상. 개인은 통과 어려움 |

### Greenhouse 에 대해

기업이 자사 채용페이지에 임베드하라고 **공개한 공식 JSON API**다. 크롤링이
아니라 문서화된 엔드포인트를 읽는 것이라 [DESIGN.md](DESIGN.md) 3.5의 방침에
걸리지 않는다.

한계는 **지정한 회사만 본다**는 것이다. 전국을 훑는 워크넷과 성격이 다르다.
현재 4곳(krafton, moloco, sendbird, daangn)에서 169건을 가져와 128건이 대상
지역이다.

보드를 늘리려면 `.env` 의 `GREENHOUSE_BOARDS` 에 추가한다. 토큰은 그 회사
채용페이지 URL(`boards.greenhouse.io/<토큰>`)에 그대로 들어 있다.

```
GREENHOUSE_BOARDS=krafton,moloco,sendbird,daangn,새회사토큰
```

---

## 7. 받는 메시지 형식

지역별로 묶여서 온다.

```
☀️ 오늘의 채용공고 3건
키워드: 백엔드, GIS, 공간정보

📍 판교·분당
▪️ 백엔드 개발자 (PostgreSQL/PostGIS)
   (주)공간정보 · 경기 성남시 분당구
   경력 5년 이상 | 6,000만원
   마감 2026-09-30

📍 서울
▪️ GIS 공간DB 엔지니어
   국토정보 · 서울 중구
```

- 제목은 공고 링크다. URL이 없으면 굵은 글씨로만 나온다.
- **한 번에 최대 12건.** 넘치면 "…외 N건. /more 로 더 보기"가 붙는다.
- 조건에 맞는 새 공고가 없으면 **아무것도 보내지 않는다.** "오늘은 없습니다"
  같은 메시지로 알림을 낭비하지 않는다.

---

## 8. 재발송 방지

같은 공고는 두 번 오지 않는다. 세 층으로 막는다.

1. **소스 내 중복** — `(source, source_id)` 유니크 제약
2. **소스 간 중복** — `dedup_key = sha1(회사명 + 직무명)`.
   워크넷과 사람인에 같은 공고가 올라와도 한 번만 간다
3. **재발송** — `deliveries` 원장. 보낸 `(사용자, dedup_key)` 를 영구 기록

`jobs` 테이블을 통째로 지워도 원장은 남기 때문에, DB를 날리고 재수집해도
이미 받은 공고가 다시 오지 않는다.

> 실제로 Supabase 이관 때 `jobs` 128건은 버리고 원장 14건만 옮겼는데,
> 이관 직후 재수집에서 재발송이 0건이었다.

---

## 9. 알려진 한계와 함정

| 문제 | 증상 | 대처 |
|---|---|---|
| **제외어가 한글만** | `인턴` 을 넣어도 "Engineer **Intern**" 이 통과 | `/exclude` 에 `intern`, `contract` 추가 |
| **부분문자열 매칭** | "BE" 로는 "백엔드" 가 안 걸림 | 표기 변형을 직접 나열 |
| **Greenhouse 보드 수동 관리** | 지정 안 한 회사 공고는 안 옴 | `GREENHOUSE_BOARDS` 추가 |
| **Hobby 크론 ±59분** | 8시 정각이 아님 | Pro 플랜 또는 감수 |
| **회사명 표기 차이** | "(주)A" 와 "A" 를 다른 회사로 봄 | 중복 발송 가능. 알려진 한계 |

---

## 10. 운영 명령 (관리자)

```bash
python -m app.main dryrun          # 수집 + 결과 출력, 발송 안 함
python -m app.main ingest          # 수집만
python -m app.main digest          # 발송만 (진짜 보냄)
python -m app.main webhook         # 봇/웹훅 상태 확인
python -m app.main webhook on      # .env 의 PUBLIC_BASE_URL 로 등록
python -m app.main webhook <URL>   # 주소 직접 지정
python -m app.main webhook off     # 웹훅 해제 (롱폴링 복귀)
python -m app.main run             # 상주 모드 (웹훅 걸려 있으면 거부됨)
```

로컬에서 실행해도 **같은 Supabase 를 본다.** 디버깅에 유용하지만 `digest` 는
진짜 발송하니 확인용으로는 `dryrun` 을 쓸 것.
