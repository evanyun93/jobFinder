# 설계문서

채용공고 알리미가 **왜 이렇게 생겼는지**를 적는다. 무엇을 하는지는
[FEATURES.md](FEATURES.md), 다른 곳으로 옮기는 방법은 [PORTING.md](PORTING.md).

---

## 1. 문제와 제약

서울·판교 생활권의 신규 채용공고를 매일 아침 텔레그램으로 받는다. 개인용, 무료.

제약이 설계를 거의 다 결정했다.

| 제약 | 결과 |
|---|---|
| **무료여야 함** | API 쿼터가 빡빡하다 (사람인 일 500회, 워크넷 일 1000회) |
| **크롤링 금지** | 공식 API 어댑터만. 아래 3.5 참고 |
| **사용자는 나 혼자** | 확장성보다 운영 단순함이 우선 |
| **PC가 꺼져 있어도 와야 함** | 상주 프로세스에 의존할 수 없다 |

마지막 제약이 2026-08-20 에 추가되면서 로컬 상주 → 서버리스로 옮겼다.

---

## 2. 아키텍처

```
                    ┌──────────── 하루 1회 (08:00 KST) ────────────┐
                    │                                              │
  [소스 어댑터]  ──▶  [지역 게이트]  ──▶  [jobs 테이블]  ──▶  [매처]  ──▶  [텔레그램]
   greenhouse         서울·판교권만        dedup 저장       사용자별 필터    다이제스트
   worknet            나머지는 버림                                          │
   saramin                                                                   ▼
   jobkorea                                                          [deliveries 원장]
                                                                      재발송 차단

  [텔레그램 웹훅] ──▶ [명령 처리] ──▶ [filters 테이블]     (수시, 사용자가 칠 때마다)
   /keywords 등
```

핵심은 **수집과 매칭이 분리**돼 있다는 것이다.

### 왜 분리했나

사용자별로 API를 때리면 호출량이 `사용자 수 × 소스 수 × 페이지 수`로 늘어난다.
일 500회 쿼터로는 몇 명 못 받는다.

대신 하루 한 번 **넓게 긁어 DB에 쌓고**, 매칭은 로컬 SQL로 한다. 그러면
호출량이 사용자 수와 무관하게 고정된다. 1명이든 1,000명이든 똑같다.

대가는 신선도다. 공고가 올라온 지 최대 24시간 뒤에 알림이 간다. 아침에 한 번
받는 서비스라 문제되지 않는다.

---

## 3. 핵심 설계 결정

### 3.1 지역 게이트를 수집 단계에 둔 이유

범위 밖 공고는 **DB에 넣지도 않는다** ([app/digest.py](app/digest.py) 의 `is_covered`).

매칭 단계에서 걸러도 결과는 같지만, 그러면 전국 공고가 DB에 쌓인다. 개인용
서비스에 그럴 이유가 없고, 무료 DB 용량도 아껴야 한다. 실측 통과율은 약 76%다
(greenhouse 169건 → 128건).

지역 정의는 [app/regions.py](app/regions.py) 한 곳에만 있다. 범위를 바꾸려면
거기만 고치면 된다.

`EXCLUDE = ("처인",)` 이 필요한 이유: "용인"으로 매칭하면 처인구까지 딸려온다.
같은 시(市)라도 생활권이 갈리는 곳이 있어 예외 목록을 따로 뒀다.

### 3.2 중복 제거 키

```python
dedup_key = sha1(정규화(회사명) + "|" + 정규화(직무명))
```

같은 공고가 워크넷과 사람인에 동시에 올라와도 한 번만 간다.

완벽하지 않다. 회사명 표기가 다르면("(주)공간정보" vs "공간정보") 다른 키가
나온다. 그래도 **오탐(다른 공고를 같다고 판단)보다 중복 발송이 덜 짜증난다**는
판단으로 보수적으로 잡았다. 정규화는 소문자화 + 괄호/구분자 제거 수준이다.

`source_id` 기준 유니크 제약은 따로 있다 — 같은 소스에서 같은 공고를 두 번
넣지 않기 위한 것이고, `dedup_key` 는 소스를 가로지르는 중복을 잡는다. 층이 다르다.

### 3.3 deliveries 원장

"보냈다"는 사실을 `(user_id, dedup_key)` 로 기록한다. 매일 재수집해도 이미 보낸
공고는 후보에서 빠진다.

`jobs` 를 지워도 원장은 남기 때문에, DB를 날리고 재수집해도 중복 발송이 없다.
실제로 Supabase 이관 때 `jobs` 는 버리고 원장만 옮겼는데, 그래서 이관 직후
재발송이 0건이었다.

`/preview` 는 원장에 남기지 않는다. 미리 본 공고가 아침에 또 와야 자연스럽다.
반면 `/more` 는 남긴다 — 잘린 나머지를 본 것이므로.

### 3.4 매칭 규칙

```
제외어 ─ 하나라도 걸리면 탈락 (제외가 항상 이긴다)
지역   ─ 하나라도 걸리면 통과 (OR). 비면 조건 없음
키워드 ─ 하나라도 걸리면 통과 (OR). 비면 아무것도 안 보냄
```

키워드가 비었을 때 전부 보내지 않고 **아무것도 안 보내는** 게 중요하다. 설정
안 한 사용자에게 전국 공고를 쏘면 즉시 차단당한다.

매칭 대상 텍스트(`haystack`)에는 **본문까지 포함**한다. 제목만 보면
"백엔드 개발자" 한 줄이라 "Python", "SQL" 같은 기술 키워드를 다 놓친다.

### 3.5 크롤링 어댑터를 넣지 않는 이유

잡코리아가 사람인을 상대로 낸 소송에서 대법원(**2017다224395**)은 무단 크롤링을
부정경쟁행위이자 데이터베이스제작자 권리 침해로 보고 **총 4억 5천만원** 배상을
확정했다. "10%만 선별해 재구성했다"는 항변도 받아들여지지 않았다.

개인용이라도 밟을 이유가 없는 지뢰다. **공식 API 또는 제공자가 배포를 허용한
피드만** `sources/` 에 들어온다. 이 규칙은 [app/sources/base.py](app/sources/base.py)
맨 위에도 적어뒀다.

이 때문에 인크루트는 붙이지 않았다 — 공식 오픈API가 없고 `robots.txt` 가
`User-agent: *` 를 전면 차단한다.

### 3.6 서버리스 + 단일 WSGI 앱

Vercel 의 Python 빌더는 `api/*.py` 를 함수 하나씩 만드는 예전 방식을 더 이상
받지 않고 **앱 하나**를 요구한다. 그래서 [server.py](server.py) 의 WSGI 앱이
두 경로를 직접 라우팅한다.

프레임워크(Flask/FastAPI)를 안 쓴 이유는 두 가지다. 라우트가 둘뿐이라 WSGI
원형으로 충분하고, 의존성이 적을수록 콜드스타트가 짧다. 그리고 **표준 WSGI라
어디로든 옮겨간다** — 이게 이식성의 핵심이다.

### 3.7 크론 한 번에 수집+발송

원래는 07:30 수집 / 08:00 발송으로 나눠져 있었다. 서버리스로 오면서 한 번의
호출에서 연달아 처리하도록 합쳤다.

Hobby 플랜의 크론이 하루 1회로 제한되는 게 직접적 이유지만, 사실 이쪽이 더
정확하다. 원래 의도가 "발송 전에 수집이 끝나 있어야 한다"였는데, 두 개로
나누면 수집이 30분 안에 끝난다는 가정에 의존하게 된다.

> 실제로 이 가정 때문에 버그가 있었다. `minute=max(0, digest_minute - 30)` 이
> 분에서 30을 뺄 때 음수를 0으로 잘라서, `DIGEST_MINUTE=0`(기본값)이면 수집과
> 발송이 **같은 분**에 돌았다. 합치면서 이 문제 자체가 사라졌다.

---

## 4. 데이터 모델

Supabase Postgres. 전체 스키마는 [app/db.py](app/db.py) 의 `SCHEMA`.

| 테이블 | 역할 | 수명 |
|---|---|---|
| `users` | chat_id ↔ 내부 id, 활성 여부 | 영구 |
| `filters` | 사용자별 키워드/지역/제외 (JSON 문자열) | 영구 |
| `jobs` | 수집된 공고 캐시 | 사실상 임시 — 지워도 재수집됨 |
| `deliveries` | 발송 원장 `(user_id, dedup_key)` | **영구. 이게 지워지면 재발송 사고** |
| `kv` | 잡다한 상태 (폴링 오프셋 등) | 웹훅 전환 후 사실상 미사용 |

`filters` 의 배열을 JSONB 가 아니라 **TEXT** 로 둔 이유: 앱이 `json.dumps` /
`json.loads` 로 다루는데, JSONB 로 하면 드라이버가 이미 list 로 돌려줘서
`json.loads` 가 터진다. 저장 형식을 바꿀 이유가 없었다.

### SQLite → Postgres 이관에서 실제로 걸린 것들

README 에 "표준 SQL 만 써서 그대로 이관 가능"이라고 적혀 있었지만 사실이 아니었다.

| SQLite | Postgres |
|---|---|
| `?` | `%s` |
| `INTEGER PRIMARY KEY AUTOINCREMENT` | `GENERATED ALWAYS AS IDENTITY` |
| `datetime('now')` | `now()` |
| `datetime('now', '-36 hours')` | `now() - make_interval(hours => %s)` |
| `INSERT OR IGNORE` | `ON CONFLICT DO NOTHING` |
| `active INTEGER 0/1` | `active BOOLEAN` |
| `SELECT * … GROUP BY dedup_key` | `SELECT DISTINCT ON (dedup_key)` |

마지막이 특히 조용한 함정이었다. SQLite 는 그룹당 아무 행이나 집어주지만
Postgres 는 문법 오류로 거절한다. `DISTINCT ON` 으로 옮기면서 "같은 공고 중
가장 최근 것"으로 선택 기준까지 명시했다 — 원래보다 나아졌다.

---

## 5. 실행 모드 세 가지

같은 코드가 세 방식으로 돈다. **이게 이식성의 근거다.**

| 모드 | 진입점 | 스케줄 | 봇 명령 | 쓰는 곳 |
|---|---|---|---|---|
| **서버리스** | `server.py` (WSGI) | 외부 크론 → `/api/cron` | 웹훅 | 현재 (Vercel) |
| **상주** | `python -m app.main run` | APScheduler | 롱폴링 | VM, 로컬 |
| **배치** | `python -m app.main ingest` + `digest` | 외부 스케줄러 | 없음 | GitHub Actions |

상주 모드를 지우지 않고 남겨둔 것은 의도적이다. 호스팅이 막혔을 때 VM 하나만
있으면 그대로 돌아간다.

웹훅과 롱폴링은 **동시에 못 쓴다**. 웹훅이 걸려 있으면 `getUpdates` 가 409로
거절되므로, `run` 은 실행 전에 웹훅 상태를 확인하고 막는다.

---

## 6. 보안 경계

로컬 PC에서 돌 때는 없던 문제들이다. 공개 URL이 생기는 순간 전부 생긴다.

| 경계 | 방어 | 실패 시 |
|---|---|---|
| `/api/cron` | `Authorization: Bearer $CRON_SECRET` | 아무나 쿼터를 태우고 중복 발송 유발 |
| `/api/telegram` | `X-Telegram-Bot-Api-Secret-Token` | 아무나 가짜 명령으로 남의 필터 변경 |
| Supabase REST | 전 테이블 RLS on + 정책 0개 + anon 권한 REVOKE | 공개 anon 키로 chat_id·키워드 유출 |
| 로그 | httpx 로거를 WARNING 으로 | 쿼리스트링의 API 키가 로그에 남음 |
| 저장소 | `.gitignore` 에 `.env` | 봇 토큰 커밋 |

두 시크릿 모두 **없으면 통과시키지 않는다**(fail closed). 설정을 깜빡한 채
배포하면 엔드포인트가 열려 있는 게 아니라 아예 안 도는 쪽이 낫다.

앱은 `postgres` 롤로 직접 접속하므로 RLS 를 우회한다. RLS 는 PostgREST 경로만
막는 용도다.

---

## 7. 호스팅 종속 지점

Vercel 에 묶인 부분은 **네 곳뿐**이다. 나머지는 표준 Python이다.

| 파일 | 종속 내용 | 다른 곳에서 |
|---|---|---|
| `vercel.json` | 크론 스케줄, maxDuration | 무시됨. 해당 호스트 방식으로 대체 |
| `pyproject.toml` `[tool.vercel]` | 진입점 선언 | 무시됨. 다른 키는 표준 |
| `.vercelignore` | 배포 제외 | 무시됨 |
| `server.py` 의 헤더 이름 | `Authorization: Bearer` 규약 | 그대로 씀. 스케줄러가 보내주면 됨 |

`server.py` 자체는 **순수 WSGI**다. gunicorn, uwsgi, waitress, wsgiref 어디서든
그대로 돈다 (표준 `wsgiref` 서버로 검증함).

DB는 이미 외부(Supabase)라 호스팅과 무관하다.

자세한 이전 절차는 [PORTING.md](PORTING.md).

---

## 8. 알려진 한계

솔직하게 적는다. 고치면 여기서 지운다.

1. **제외어가 한글만 걸린다.** 부분문자열 매칭이라 "Machine Learning Engineer
   **Intern**" 이 `인턴` 필터를 통과한다. 사용자가 `intern` 을 직접 추가해야 한다.
2. **키워드 매칭이 부분문자열이다.** "BE" ≠ "백엔드", "PostgreSQL" ≠ "포스트그레".
   임베딩 유사도가 다음 단계다.
3. **Greenhouse 보드 목록을 수동 관리한다.** 전국을 훑는 워크넷과 달리 지정한
   회사만 본다. `GREENHOUSE_BOARDS` 를 직접 늘려야 커버리지가 는다.
4. **워크넷 키가 거부되고 있다.** 엔드포인트와 파라미터는 맞는데 키가 반려된다
   (`messageCd 002`). 활용신청 승인 상태를 확인해야 한다.
5. **잡코리아 어댑터가 두 벌이다.** `app/sources/jobkorea.py`(코드표 방식)와
   `legacy/jobkorea.py`(필드맵+probe 방식). 승인되면 하나로 정리해야 한다.
6. **Hobby 크론은 ±59분 오차**가 있다. 8:00~8:59 사이 아무 때나 온다.
7. **함수가 미국(iad1)에서 돈다.** DB는 서울이라 쿼리마다 태평양을 왕복한다.
   현재 11.6초로 여유는 있지만 리전을 맞추면 빨라진다.
